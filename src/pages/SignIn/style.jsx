import styled from "styled-components";
const Styled = styled.div`
  .signin-container {
  }
  .header {
    position: fixed;
    left: 0px;
    top: 0px;
    z-index: 2;
  }
`;

export default Styled;
